오늘 응프시간에 개발자 모드를 켜서 보던 중 내가 클릭한 항목에 == $0 이라고 뜨는게 보였다.
과연 == $0이 의미하는 바는 무엇일까
### == $0
== $0이란? 
    - 현재 선택한 HTML 요소를 자바스크립트 콘솔에서 참조할 수 있는 변수이다.
    - 즉 내가 클릭한 코드가 어디인지 알려주는 그런거다.
    - $0은 가장 최근 클릭한 요소를 가리키고, $1은 그 전, $2는 그 전전 요소를 가리키는 방식으로 이전 선택 요소들을 저장한다.